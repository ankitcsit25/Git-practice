#include <stdio.h>

int main(){
    int a,b,mul;
    printf("Enter a:");
    scanf("%d", &a);
    printf("Enter b:");
    scanf("%d", &b);
    mul = a * b;
    printf("The product of two numbers is %d", mul);
    return 0;
}